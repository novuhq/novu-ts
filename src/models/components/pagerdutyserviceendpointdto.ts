/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * PagerDuty account region — determines the events API data-center endpoint.
 */
export const Region = {
  Us: "us",
  Eu: "eu",
} as const;
/**
 * PagerDuty account region — determines the events API data-center endpoint.
 */
export type Region = ClosedEnum<typeof Region>;

export type PagerDutyServiceEndpointDto = {
  /**
   * PagerDuty Events API v2 integration key (32-character alphanumeric string). Stored encrypted on the linked channel connection.
   */
  routingKey: string;
  /**
   * PagerDuty account region — determines the events API data-center endpoint.
   */
  region: Region;
};

/** @internal */
export const Region$inboundSchema: z.ZodNativeEnum<typeof Region> = z
  .nativeEnum(Region);
/** @internal */
export const Region$outboundSchema: z.ZodNativeEnum<typeof Region> =
  Region$inboundSchema;

/** @internal */
export const PagerDutyServiceEndpointDto$inboundSchema: z.ZodType<
  PagerDutyServiceEndpointDto,
  z.ZodTypeDef,
  unknown
> = z.object({
  routingKey: z.string(),
  region: Region$inboundSchema,
});
/** @internal */
export type PagerDutyServiceEndpointDto$Outbound = {
  routingKey: string;
  region: string;
};

/** @internal */
export const PagerDutyServiceEndpointDto$outboundSchema: z.ZodType<
  PagerDutyServiceEndpointDto$Outbound,
  z.ZodTypeDef,
  PagerDutyServiceEndpointDto
> = z.object({
  routingKey: z.string(),
  region: Region$outboundSchema,
});

export function pagerDutyServiceEndpointDtoToJSON(
  pagerDutyServiceEndpointDto: PagerDutyServiceEndpointDto,
): string {
  return JSON.stringify(
    PagerDutyServiceEndpointDto$outboundSchema.parse(
      pagerDutyServiceEndpointDto,
    ),
  );
}
export function pagerDutyServiceEndpointDtoFromJSON(
  jsonString: string,
): SafeParseResult<PagerDutyServiceEndpointDto, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PagerDutyServiceEndpointDto$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PagerDutyServiceEndpointDto' from JSON`,
  );
}
