/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { layoutsDelete } from "../../funcs/layoutsDelete.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  layoutId: z.string(),
  idempotencyKey: z.string().optional(),
};

export const tool$layoutsDelete: ToolDefinition<typeof args> = {
  name: "layouts-delete",
  description: `Delete a layout

Removes a specific layout by its unique identifier **layoutId**`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await layoutsDelete(
      client,
      args.layoutId,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
