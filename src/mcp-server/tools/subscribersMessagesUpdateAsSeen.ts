/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscribersMessagesUpdateAsSeen } from "../../funcs/subscribersMessagesUpdateAsSeen.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.SubscribersV1ControllerMarkActionAsSeenRequest$inboundSchema,
};

export const tool$subscribersMessagesUpdateAsSeen: ToolDefinition<typeof args> =
  {
    name: "subscribers-messages_update-as-seen",
    description: `Mark message action as seen`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await subscribersMessagesUpdateAsSeen(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
