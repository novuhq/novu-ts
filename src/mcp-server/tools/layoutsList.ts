/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { layoutsList } from "../../funcs/layoutsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.LayoutsControllerListRequest$inboundSchema,
};

export const tool$layoutsList: ToolDefinition<typeof args> = {
  name: "layouts-list",
  description: `List all layouts

Retrieves a list of layouts with optional filtering and pagination`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await layoutsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
