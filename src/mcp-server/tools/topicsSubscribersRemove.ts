/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { topicsSubscribersRemove } from "../../funcs/topicsSubscribersRemove.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  removeSubscribersRequestDto:
    components.RemoveSubscribersRequestDto$inboundSchema,
  topicKey: z.string(),
  idempotencyKey: z.string().optional(),
};

export const tool$topicsSubscribersRemove: ToolDefinition<typeof args> = {
  name: "topics-subscribers_remove",
  description: `Subscribers removal

Remove subscribers from a topic`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await topicsSubscribersRemove(
      client,
      args.removeSubscribersRequestDto,
      args.topicKey,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
