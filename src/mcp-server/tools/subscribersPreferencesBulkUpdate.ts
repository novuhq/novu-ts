/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { subscribersPreferencesBulkUpdate } from "../../funcs/subscribersPreferencesBulkUpdate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  bulkUpdateSubscriberPreferencesDto:
    components.BulkUpdateSubscriberPreferencesDto$inboundSchema,
  subscriberId: z.string(),
  idempotencyKey: z.string().optional(),
};

export const tool$subscribersPreferencesBulkUpdate: ToolDefinition<
  typeof args
> = {
  name: "subscribers-preferences-bulk-update",
  description: `Bulk update subscriber preferences

Bulk update subscriber preferences by its unique key identifier **subscriberId**. 
    This API allows updating multiple workflow preferences in a single request.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await subscribersPreferencesBulkUpdate(
      client,
      args.bulkUpdateSubscriberPreferencesDto,
      args.subscriberId,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
