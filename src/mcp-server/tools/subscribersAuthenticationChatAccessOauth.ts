/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscribersAuthenticationChatAccessOauth } from "../../funcs/subscribersAuthenticationChatAccessOauth.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.SubscribersV1ControllerChatAccessOauthRequest$inboundSchema,
};

export const tool$subscribersAuthenticationChatAccessOauth: ToolDefinition<
  typeof args
> = {
  name: "subscribers-authentication_chat-access-oauth",
  description: `Handle chat oauth`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await subscribersAuthenticationChatAccessOauth(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
