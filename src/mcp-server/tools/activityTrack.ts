/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { activityTrack } from "../../funcs/activityTrack.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.InboundWebhooksControllerHandleWebhookRequest$inboundSchema,
};

export const tool$activityTrack: ToolDefinition<typeof args> = {
  name: "activity-track",
  description: `Track activity and engagement events

Track activity and engagement events for a specific delivery provider`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await activityTrack(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
