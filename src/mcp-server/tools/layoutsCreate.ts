/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { layoutsCreate } from "../../funcs/layoutsCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  createLayoutDto: components.CreateLayoutDto$inboundSchema,
  idempotencyKey: z.string().optional(),
};

export const tool$layoutsCreate: ToolDefinition<typeof args> = {
  name: "layouts-create",
  description: `Create a layout

Creates a new layout in the Novu Cloud environment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await layoutsCreate(
      client,
      args.createLayoutDto,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
