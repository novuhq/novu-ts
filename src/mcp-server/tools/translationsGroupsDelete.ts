/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { translationsGroupsDelete } from "../../funcs/translationsGroupsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  resourceType: operations.ResourceType$inboundSchema,
  resourceId: z.string(),
  idempotencyKey: z.string().optional(),
};

export const tool$translationsGroupsDelete: ToolDefinition<typeof args> = {
  name: "translations-groups-delete",
  description: `Delete translation group

Delete an entire translation group and all its translations`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await translationsGroupsDelete(
      client,
      args.resourceType,
      args.resourceId,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
