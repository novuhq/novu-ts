/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { layoutsRetrieve } from "../../funcs/layoutsRetrieve.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  layoutId: z.string(),
  idempotencyKey: z.string().optional(),
};

export const tool$layoutsRetrieve: ToolDefinition<typeof args> = {
  name: "layouts-retrieve",
  description: `Retrieve a layout

Fetches details of a specific layout by its unique identifier **layoutId**`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await layoutsRetrieve(
      client,
      args.layoutId,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
