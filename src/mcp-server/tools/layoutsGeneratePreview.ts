/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { layoutsGeneratePreview } from "../../funcs/layoutsGeneratePreview.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  layoutPreviewRequestDto: components.LayoutPreviewRequestDto$inboundSchema,
  layoutId: z.string(),
  idempotencyKey: z.string().optional(),
};

export const tool$layoutsGeneratePreview: ToolDefinition<typeof args> = {
  name: "layouts-generate-preview",
  description: `Generate layout preview

Generates a preview for a layout by its unique identifier **layoutId**`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await layoutsGeneratePreview(
      client,
      args.layoutPreviewRequestDto,
      args.layoutId,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
