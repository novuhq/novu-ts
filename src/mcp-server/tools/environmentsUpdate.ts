/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { environmentsUpdate } from "../../funcs/environmentsUpdate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  updateEnvironmentRequestDto:
    components.UpdateEnvironmentRequestDto$inboundSchema,
  environmentId: z.string(),
  idempotencyKey: z.string().optional(),
};

export const tool$environmentsUpdate: ToolDefinition<typeof args> = {
  name: "environments-update",
  description: `Update an environment

Update an environment by its unique identifier **environmentId**. 
    You can modify the environment name, identifier, color, and other configuration settings.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await environmentsUpdate(
      client,
      args.updateEnvironmentRequestDto,
      args.environmentId,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
