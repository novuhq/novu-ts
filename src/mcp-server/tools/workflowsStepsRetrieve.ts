/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { workflowsStepsRetrieve } from "../../funcs/workflowsStepsRetrieve.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  workflowId: z.string(),
  stepId: z.string(),
  idempotencyKey: z.string().optional(),
};

export const tool$workflowsStepsRetrieve: ToolDefinition<typeof args> = {
  name: "workflows-steps-retrieve",
  description: `Retrieve workflow step

Retrieves data for a specific step in a workflow`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workflowsStepsRetrieve(
      client,
      args.workflowId,
      args.stepId,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
