/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { workflowsUpdate } from "../../funcs/workflowsUpdate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  updateWorkflowDto: components.UpdateWorkflowDto$inboundSchema,
  workflowId: z.string(),
  idempotencyKey: z.string().optional(),
};

export const tool$workflowsUpdate: ToolDefinition<typeof args> = {
  name: "workflows-update",
  description: `Update a workflow

Updates the details of an existing workflow, here **workflowId** is the identifier of the workflow`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workflowsUpdate(
      client,
      args.updateWorkflowDto,
      args.workflowId,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
