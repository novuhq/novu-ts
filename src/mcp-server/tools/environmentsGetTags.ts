/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { environmentsGetTags } from "../../funcs/environmentsGetTags.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  environmentId: z.string(),
  idempotencyKey: z.string().optional(),
};

export const tool$environmentsGetTags: ToolDefinition<typeof args> = {
  name: "environments-get-tags",
  description: `Get environment tags

Retrieve all unique tags used in workflows within the specified environment. These tags can be used for filtering workflows.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await environmentsGetTags(
      client,
      args.environmentId,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
