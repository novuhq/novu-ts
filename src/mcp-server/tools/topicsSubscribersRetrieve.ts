/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { topicsSubscribersRetrieve } from "../../funcs/topicsSubscribersRetrieve.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  externalSubscriberId: z.string(),
  topicKey: z.string(),
  idempotencyKey: z.string().optional(),
};

export const tool$topicsSubscribersRetrieve: ToolDefinition<typeof args> = {
  name: "topics-subscribers_retrieve",
  description: `Check topic subscriber

Check if a subscriber belongs to a certain topic`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await topicsSubscribersRetrieve(
      client,
      args.externalSubscriberId,
      args.topicKey,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
