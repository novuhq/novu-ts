/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscribersAuthenticationChatAccessOauthCallBack } from "../../funcs/subscribersAuthenticationChatAccessOauthCallBack.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.SubscribersV1ControllerChatOauthCallbackRequest$inboundSchema,
};

export const tool$subscribersAuthenticationChatAccessOauthCallBack:
  ToolDefinition<typeof args> = {
    name: "subscribers-authentication-chat-access-oauth-call-back",
    description: `Handle providers oauth redirect`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await subscribersAuthenticationChatAccessOauthCallBack(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
