/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { layoutsDuplicate } from "../../funcs/layoutsDuplicate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  duplicateLayoutDto: components.DuplicateLayoutDto$inboundSchema,
  layoutId: z.string(),
  idempotencyKey: z.string().optional(),
};

export const tool$layoutsDuplicate: ToolDefinition<typeof args> = {
  name: "layouts-duplicate",
  description: `Duplicate a layout

Duplicates a layout by its unique identifier **layoutId**. This will create a new layout with the content of the original layout.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await layoutsDuplicate(
      client,
      args.duplicateLayoutDto,
      args.layoutId,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
