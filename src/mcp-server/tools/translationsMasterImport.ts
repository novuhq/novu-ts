/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { translationsMasterImport } from "../../funcs/translationsMasterImport.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  importMasterJsonRequestDto:
    components.ImportMasterJsonRequestDto$inboundSchema,
  idempotencyKey: z.string().optional(),
};

export const tool$translationsMasterImport: ToolDefinition<typeof args> = {
  name: "translations-master-import",
  description: `Import master translations JSON

Import translations from master JSON format for a specific locale`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await translationsMasterImport(
      client,
      args.importMasterJsonRequestDto,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
