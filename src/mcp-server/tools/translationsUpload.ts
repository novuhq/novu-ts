/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { translationsUpload } from "../../funcs/translationsUpload.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  uploadTranslationsRequestDto:
    components.UploadTranslationsRequestDto$inboundSchema,
  idempotencyKey: z.string().optional(),
};

export const tool$translationsUpload: ToolDefinition<typeof args> = {
  name: "translations-upload",
  description: `Upload translation files

Upload one or more JSON translation files for a specific workflow. Files name must match the locale, e.g. en_US.json`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await translationsUpload(
      client,
      args.uploadTranslationsRequestDto,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
