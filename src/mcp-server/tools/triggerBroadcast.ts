/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { triggerBroadcast } from "../../funcs/triggerBroadcast.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  triggerEventToAllRequestDto:
    components.TriggerEventToAllRequestDto$inboundSchema,
  idempotencyKey: z.string().optional(),
};

export const tool$triggerBroadcast: ToolDefinition<typeof args> = {
  name: "trigger-broadcast",
  description: `Broadcast event to all

Trigger a broadcast event to all existing subscribers, could be used to send announcements, etc.
      In the future could be used to trigger events to a subset of subscribers based on defined filters.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await triggerBroadcast(
      client,
      args.triggerEventToAllRequestDto,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
