/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { workflowsCreate } from "../../funcs/workflowsCreate.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  createWorkflowDto: components.CreateWorkflowDto$inboundSchema,
  idempotencyKey: z.string().optional(),
};

export const tool$workflowsCreate: ToolDefinition<typeof args> = {
  name: "workflows-create",
  description: `Create a workflow

Creates a new workflow in the Novu Cloud environment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workflowsCreate(
      client,
      args.createWorkflowDto,
      args.idempotencyKey,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
